import {TodolistType} from "../../App";

export const todolistsReducer = (state: TodolistType[], action: any) => {
    switch (action.type) {
        case '': {
            return state
        }
        default: {
            return state
        }
    }
}